void MyStack ::push(int x) {
    // Your Code
    top++;
    arr[top] = x;
    return ;
}

// Function to remove an item from top of the stack.
int MyStack ::pop() {
    // Your Code
    int p = top;
    top--;
    if(p<0)return -1;
    return arr[p];
}
